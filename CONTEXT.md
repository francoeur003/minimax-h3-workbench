# Domain Context

本项目把 MiniMax H3 的部署和生成复杂度封装为普通用户可理解的桌面工作台。

核心术语：

- 就绪中心：检测电脑、ComfyUI、模型和执行连接的入口。
- 执行后端：本地 ComfyUI、SSH 远程 ComfyUI、MiniMax 云 API。
- 生成任务：一次生成请求；四宫格对应四个独立子任务。
- FL2VA：文生、首帧、尾帧和首尾帧工作流。
- Ref2VA：参考图、参考视频和参考音频工作流。
- 预检：用短样片验证模型确实可以加载和生成。

代码设计遵循：界面只调用统一的生成接口，具体后端差异由适配器封装。
