# MiniMax H3 工作台 PRD 自审记录

> 审核日期：2026-08-07
> 审核对象：[PRD.md](./PRD.md)
> 审核结论：通过，可进入产品评审；仍有 8 项产品决策需在开发前确认。

## 1. 对用户意图的最终理解

用户要的是一套可以交付给不会部署模型的普通用户使用的 MiniMax H3 整合工作台，而不是只把 H3 接口包一层 UI。

完整意图包含五件事：

1. 工作台先判断当前电脑到底适不适合跑 H3；
2. 把模型、工作流和 ComfyUI 的下载地址及目录规则内置，用户点击即可自动安装；
3. 把安装、配置、SSH 和常见问题说明放在工作台内；
4. 视频创作页固定左侧输入、右侧四个候选结果；
5. 同一套 UI 既能调用 H3，也能连接用户租用的远程显卡。

核心体验目标：用户不需要理解模型分区、节点图、CUDA 命令和 SSH 隧道，也能生成视频。

## 2. 原始需求覆盖检查

| 用户需求 | PRD 覆盖 | 审核结果 |
|---|---|---|
| 检测电脑能否运行 H3 | 环境检测、四档结论、5 秒真实预检 | 通过 |
| 内嵌下载，点击自动下载 | 下载套餐、官方直链、自动目录映射、断点与校验 | 通过 |
| 整合包和 ComfyUI 配置指南 | 内嵌知识库、上下文帮助、一键准备环境 | 通过 |
| 左侧输入、右侧四个展示框 | 创作台 360–420px 左栏、右侧 2×2 四宫格 | 通过 |
| 文生视频并支持首尾帧 | 文生视频模式内置首帧/尾帧控制 | 通过 |
| 图生视频 | 单图起始条件和多图参考 | 通过 |
| 视频生视频 | Ref2VA 参考视频模式 | 通过 |
| 对接 H3 接口 | 本地 ComfyUI 与 MiniMax 云 API 适配器 | 通过 |
| 增加 SSH 接口 | SSH 检测、安装、隧道、任务恢复和 SFTP | 通过 |

## 3. 本次自审发现并修复的问题

### 问题 1：一键使用目标不够突出

原稿虽然包含检测、下载和预检，但分散在不同章节，容易被开发成多个互不相连的工具页。

已修复：新增“用户意图还原”“就绪中心”和“一键准备环境”，把全部步骤定义成一条可恢复的主流程。

### 问题 2：内嵌下载只写了文件名，没有把直链逐项写死

用户要求的是点击即可自动下载，原稿的产品表达仍可能被理解为只放一个 Hugging Face 仓库入口。

已修复：增加 5 个模型文件和 3 个官方工作流的直接下载地址、自动落盘路径及版本化下载清单要求。

### 问题 3：模式命名没有完全遵循用户原话

原稿使用“文生/首尾帧”标签，容易让用户以为首尾帧是独立模式。

已修复：固定为“文生视频、图生视频、视频生视频”三个模式，首尾帧作为文生视频中的可选控制区。

### 问题 4：工程约束多，普通用户成功标准不够明确

原稿强调 API、队列、安全和部署，但没有把“不打开终端、不手动移动文件”写成硬验收。

已修复：将其加入产品原则、P0 和验收标准。

## 4. 自审后确认的关键产品判断

- “能不能跑”不能只看显存数字，必须用配置检测加真实预检，否则会误导消费级显卡用户。
- 四个展示框必须对应四个独立任务，本地默认排队，避免一次并发四个任务导致 OOM。
- SSH 负责安全连接远程 GPU；生成仍调用隧道后的 ComfyUI/SGLang 接口。
- 本地版和云端版能力不能混写：当前完整纯本地 2K 不应承诺。
- 模型权重不能直接塞进工作台安装包，应在许可确认后按需下载。
- V2V 是参考重生成，不是逐帧视频编辑，界面必须提前说明。

## 5. 仍需产品评审的事项

以下问题不会阻碍 PRD 评审，但会影响开发范围：

1. Windows 是否为唯一首发本地平台；
2. MiniMax 云 API 是否首发开放；
3. 首发目标市场和许可证地域门禁策略；
4. 工作台自带 ComfyUI，还是兼容用户已有安装；
5. 首批承诺支持的 GPU/内存组合；
6. 云 API 单次确认和每日费用上限；
7. 是否提供下载镜像；
8. 桌面技术栈选择。

## 6. 最终结论

修订后的 PRD 已经对齐用户真实意图：它定义的是一个“让 H3 从部署工程变成普通工具”的工作台，而不是一个面向技术人员的 ComfyUI 控制面板。

可以进入下一步：信息架构图、关键页面线框图和技术方案评审。
