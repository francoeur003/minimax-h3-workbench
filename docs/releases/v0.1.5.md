# MiniMax H3 工作台 v0.1.5

本次更新移除不应出现在公开产品中的第三方视频生成接口。

## 调整

- 移除生成后端下拉中的第三方视频接口。
- 移除连接设置、配置指南和计费提示中的相关入口与文案。
- 删除对应的连接器代码、账号字段和接口配置。
- 默认生成后端恢复为本机 ComfyUI；旧版保存的相关默认值会自动迁移为本机 ComfyUI。
- 四条内置成片继续保留，文件名和展示信息均改为中性内容，不标注生成来源。

此外，构建流程现在会先清理历史产物，避免已经删除的连接器被旧编译文件带入安装包。
